<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <ResourceDictionary.ThemeDictionaries>
    <ResourceDictionary x:Key="Default">
      <SolidColorBrush x:Key="SettingsShellBackgroundBrush" Color="#FFF3F3F3"/>
      <SolidColorBrush x:Key="SettingsSidebarBackgroundBrush" Color="#FFFAFAFA"/>
      <SolidColorBrush x:Key="SettingsContentBackgroundBrush" Color="#FFF9F9F9"/>
      <SolidColorBrush x:Key="SettingsSurfaceBrush" Color="#FFFFFFFF"/>
      <SolidColorBrush x:Key="SettingsSurfaceAltBrush" Color="#FFF6F6F6"/>
      <SolidColorBrush x:Key="SettingsAccentSoftBrush" Color="#150078D4"/>
      <SolidColorBrush x:Key="SettingsAccentOutlineBrush" Color="#260078D4"/>
      <SolidColorBrush x:Key="SettingsBorderBrush" Color="#1A000000"/>
      <SolidColorBrush x:Key="SettingsMutedTextBrush" Color="#99000000"/>
      <SolidColorBrush x:Key="SettingsStrongTextBrush" Color="#E4000000"/>
    </ResourceDictionary>
    <ResourceDictionary x:Key="Light">
      <SolidColorBrush x:Key="SettingsShellBackgroundBrush" Color="#FFF3F3F3"/>
      <SolidColorBrush x:Key="SettingsSidebarBackgroundBrush" Color="#FFFAFAFA"/>
      <SolidColorBrush x:Key="SettingsContentBackgroundBrush" Color="#FFF9F9F9"/>
      <SolidColorBrush x:Key="SettingsSurfaceBrush" Color="#FFFFFFFF"/>
      <SolidColorBrush x:Key="SettingsSurfaceAltBrush" Color="#FFF6F6F6"/>
      <SolidColorBrush x:Key="SettingsAccentSoftBrush" Color="#150078D4"/>
      <SolidColorBrush x:Key="SettingsAccentOutlineBrush" Color="#260078D4"/>
      <SolidColorBrush x:Key="SettingsBorderBrush" Color="#1A000000"/>
      <SolidColorBrush x:Key="SettingsMutedTextBrush" Color="#99000000"/>
      <SolidColorBrush x:Key="SettingsStrongTextBrush" Color="#E4000000"/>
    </ResourceDictionary>
    <ResourceDictionary x:Key="Dark">
      <SolidColorBrush x:Key="SettingsShellBackgroundBrush" Color="#FF202020"/>
      <SolidColorBrush x:Key="SettingsSidebarBackgroundBrush" Color="#FF262626"/>
      <SolidColorBrush x:Key="SettingsContentBackgroundBrush" Color="#FF1F1F1F"/>
      <SolidColorBrush x:Key="SettingsSurfaceBrush" Color="#FF2C2C2C"/>
      <SolidColorBrush x:Key="SettingsSurfaceAltBrush" Color="#FF323232"/>
      <SolidColorBrush x:Key="SettingsAccentSoftBrush" Color="#220078D4"/>
      <SolidColorBrush x:Key="SettingsAccentOutlineBrush" Color="#330078D4"/>
      <SolidColorBrush x:Key="SettingsBorderBrush" Color="#33FFFFFF"/>
      <SolidColorBrush x:Key="SettingsMutedTextBrush" Color="#B3FFFFFF"/>
      <SolidColorBrush x:Key="SettingsStrongTextBrush" Color="#F3FFFFFF"/>
    </ResourceDictionary>
  </ResourceDictionary.ThemeDictionaries>

  <Style x:Key="SubtleButtonStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="10,7"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="6"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SettingsSurfaceBorderStyle" TargetType="Border">
    <Setter Property="Background" Value="{ThemeResource SettingsSurfaceBrush}"/>
    <Setter Property="BorderBrush" Value="{ThemeResource SettingsBorderBrush}"/>
    <Setter Property="BorderThickness" Value="1"/>
    <Setter Property="CornerRadius" Value="16"/>
  </Style>

  <Style x:Key="SettingsNavButtonStyle" TargetType="RadioButton">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Foreground" Value="{ThemeResource SettingsStrongTextBrush}"/>
    <Setter Property="MinHeight" Value="56"/>
    <Setter Property="Padding" Value="14,12"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="UseSystemFocusVisuals" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="RadioButton">
          <Grid Background="Transparent">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Background" Value="{ThemeResource SettingsSurfaceAltBrush}"/>
                    <Setter Target="ContainerBorder.BorderBrush" Value="{ThemeResource SettingsBorderBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Background" Value="{ThemeResource SettingsSurfaceAltBrush}"/>
                    <Setter Target="ContainerBorder.BorderBrush" Value="{ThemeResource SettingsBorderBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Opacity" Value="0.45"/>
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
              <VisualStateGroup x:Name="CheckStates">
                <VisualState x:Name="Checked">
                  <VisualState.Setters>
                    <Setter Target="SelectionBar.Opacity" Value="1"/>
                    <Setter Target="ContainerBorder.Background" Value="{ThemeResource SettingsAccentSoftBrush}"/>
                    <Setter Target="ContainerBorder.BorderBrush" Value="{ThemeResource SettingsAccentOutlineBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Unchecked"/>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="4"/>
              <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border x:Name="SelectionBar"
                    Grid.Column="0"
                    Margin="0,8,0,8"
                    CornerRadius="99"
                    Background="{ThemeResource AccentFillColorDefaultBrush}"
                    Opacity="0"/>

            <Border x:Name="ContainerBorder"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    Background="Transparent"
                    BorderBrush="Transparent"
                    BorderThickness="1"
                    CornerRadius="10">
              <ContentPresenter Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Border>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="SettingsQuickActionStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderBrush" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="18,16"/>
    <Setter Property="Foreground" Value="{ThemeResource SettingsStrongTextBrush}"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
    <Setter Property="VerticalAlignment" Value="Stretch"/>
    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
    <Setter Property="MinHeight" Value="152"/>
    <Setter Property="UseSystemFocusVisuals" Value="False"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="ContainerBorder"
                  Background="{ThemeResource SettingsSurfaceBrush}"
                  BorderBrush="{ThemeResource SettingsBorderBrush}"
                  BorderThickness="1"
                  CornerRadius="14"
                  Padding="{TemplateBinding Padding}">
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Background" Value="{ThemeResource SettingsSurfaceAltBrush}"/>
                    <Setter Target="ContainerBorder.BorderBrush" Value="{ThemeResource SettingsBorderBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Background" Value="{ThemeResource SettingsAccentSoftBrush}"/>
                    <Setter Target="ContainerBorder.BorderBrush" Value="{ThemeResource SettingsAccentOutlineBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="ContainerBorder.Opacity" Value="0.45"/>
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="ContextMenuItemStyle" TargetType="Button">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="HorizontalAlignment" Value="Stretch"/>
    <Setter Property="HorizontalContentAlignment" Value="Left"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/>
    <Setter Property="Height" Value="32"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Grid>
            <VisualStateManager.VisualStateGroups>
              <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>
                <VisualState x:Name="PointerOver">
                  <VisualState.Setters>
                    <Setter Target="HoverRect.Opacity" Value="1"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Pressed">
                  <VisualState.Setters>
                    <Setter Target="HoverRect.Opacity" Value="1"/>
                    <Setter Target="HoverRect.Fill" Value="{ThemeResource SubtleFillColorTertiaryBrush}"/>
                  </VisualState.Setters>
                </VisualState>
                <VisualState x:Name="Disabled">
                  <VisualState.Setters>
                    <Setter Target="Content.Opacity" Value="0.4"/>
                  </VisualState.Setters>
                </VisualState>
              </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Rectangle x:Name="HoverRect"
                       RadiusX="4"
                       RadiusY="4"
                       Opacity="0"
                       Margin="4,0">
              <Rectangle.Fill>
                <AcrylicBrush TintColor="{ThemeResource SolidBackgroundFillColorBase}"
                              TintOpacity="0.35"
                              FallbackColor="{ThemeResource SubtleFillColorSecondaryBrush}"/>
              </Rectangle.Fill>
            </Rectangle>

            <ContentPresenter x:Name="Content"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Padding="{TemplateBinding Padding}"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
